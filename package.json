{
	"name": "@solidbasisventures/intelliwaketsfoundation",
	"version": "5.2.10",
	"description": "NPM package for IntelliWake TypeScript Foundation Components",
	"type": "module",
	"main": "./dist/main.umd.cjs",
	"module": "./dist/main.js",
	"types": "./dist/main.d.ts",
	"exports": {
		".": {
			"import": "./dist/main.js",
			"require": "./dist/main.cjs"
		}
	},
	"scripts": {
		"Vitest-Watch": "vitest",
		"Publish": "tsc && vite build && pnpm add -g pnpm && pnpm version patch && git commit -am \"Updated Version\" && git push && npm publish ./dist && git push",
		"Install-IntelliWake": "pnpm add -g pnpm && pnpm install",
		"BuildAndPublishOLD": "rm -rf dist/* && rollup -c && npm version patch --no-git-tag-version && npm publish",
		"Version-Minor-Advance": "pnpm version minor"
	},
	"files": [
		"dist"
	],
	"author": "SolidBasisVentures",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/SolidBasisVentures/IntelliWakeTSFoundation.git"
	},
	"devDependencies": {
		"@types/node": "^14.0.5",
		"prettier": "^2.0.5",
		"ts-node": "^9.0.0",
		"ts-node-dev": "^1.0.0",
		"typescript": "^4.0.5",
		"vite": "^4.0.0",
		"vite-plugin-dts": "^1.7.1",
		"vitest": "^0.28.3"
	}
}
